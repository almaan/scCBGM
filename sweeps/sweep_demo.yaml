program: main.py
method: grid
metric:
  goal: maximize
  name: AVG_ACC
parameters:
  model:
    values: [cb_vae, vae]
  modify:
    values: [add, noise, drop]


command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}
