defaults:
  - intervention_labels: liver/b_cells

_target_: conceptlab.data.dataset.InterventionDataset
data_path: /braid/havivd/liver_doses/SCP1871/adata_liver.h5ad
concept_key: concepts
intervention_labels: ${intervention_labels}
mmd_label: "L2_dose"
target_sum: 1214 # target sum for normalization (target_sum = np.median(adata.X.sum(axis=1)) if isinstance(adata.X, np.ndarray) else np.median(adata.X.toarray().sum(axis=1)))
concepts_as_cov: cell_types_L1 # column in obs that recapitulates the concepts \ intervention (for scGen)
soft_concept_key: soft_concepts
hard_concept_key: hard_concepts
