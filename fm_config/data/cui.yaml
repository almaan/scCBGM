defaults:
  - intervention_labels: cui/cd4

_target_: conceptlab.data.dataset.InterventionDataset
data_path: /braid/havivd/immune_dictionary/lig_seurat_with_concepts_v2.h5ad
concept_key: concepts
intervention_labels: ${intervention_labels}
mmd_label: "L1_stim"
single_cell_preproc: false
target_sum: 0 # target sum for normalization (target_sum = np.median(adata.X.sum(axis=1)) if isinstance(adata.X, np.ndarray) else np.median(adata.X.toarray().sum(axis=1)))
