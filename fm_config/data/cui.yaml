defaults:
  - intervention_labels: cui/cd4

_target_: conceptlab.data.dataset.InterventionDataset
data_path: /braid/havivd/immune_dictionary/lig_seurat_v2_with_raw_with_concepts.h5ad
concept_key: concepts
intervention_labels: ${intervention_labels}
mmd_label: "L1_stim"
single_cell_preproc: true
target_sum: null # target sum for normalization (target_sum = np.median(adata.X.sum(axis=1)) if isinstance(adata.X, np.ndarray) else np.median(adata.X.toarray().sum(axis=1)))
concepts_as_cov: cell_type_L1 # column that recapitulates the concepts \ intervention (for scGen)
soft_concept_key: null
hard_concept_key: concepts 